import React from 'react';
import Login from '../Login';

const Signin = () => (
  <div className="signin" style={{ paddingBottom: '68px' }}>
    <Login titulo="Sign in" passwordConfirmation />
  </div>
);

export default Signin;
